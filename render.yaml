services:
  - type: web
    name: editools
    runtime: docker
    plan: free
    region: oregon
    healthCheckPath: /api/health
    envVars:
      - key: MAX_FILESIZE
        value: 2G
      - key: MAX_DURATION_SECONDS
        value: '7200'
      - key: MAX_CONCURRENT_JOBS
        value: '1'
